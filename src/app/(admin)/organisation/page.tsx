export default function OrganisationPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-brand-900 tracking-tight">Organisation</h1>
      <p className="text-sm text-[var(--muted-foreground)] mt-1">Organisation profile — connect backend to populate.</p>
    </div>
  );
}
